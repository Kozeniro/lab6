pipeline {
    agent any
    
	triggers {
        pollSCM('H/3 * * * *') 
	}
	
    
    stages {
        stage('Deploy') {
            steps {
                echo "Deploying..."
                
                sshPublisher(
					 publishers: [
						 sshPublisherDesc(
							 configName: "Prod",
							 transfers: [
								sshTransfer(sourceFiles: "main.py")
							 ]
						 )
					 ]
                )
            }
        }
        
        stage('Clean') {
            steps {
                cleanWs() 
            }
        }
    }
}